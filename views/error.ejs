<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Barter Trading</title>
    <!-- Materialize CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <!-- Material Icons -->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <!-- Navigation -->
    <%- include('partials/navbar') %>

    <main style="min-height: calc(100vh - 200px); display: flex; align-items: center; justify-content: center; padding: 2rem;">
        <div class="container">
            <div class="row">
                <div class="col s12 m8 offset-m2">
                    <div class="card">
                        <div class="card-content">
                            <div style="text-align: center; margin-bottom: 2rem;">
                                <i class="material-icons" style="font-size: 4rem; color: #ff9800;">error_outline</i>
                            </div>
                            <h4 class="card-title center-align" style="margin-bottom: 1.5rem;">
                                <%= error.statusCode ? `Error ${error.statusCode}` : 'Oops!' %>
                            </h4>
                            <p class="center-align" style="font-size: 1.2rem; color: #666; margin-bottom: 2rem;">
                                <%= msg %>
                            </p>
                            <% if (process.env.NODE_ENV === 'development' && error.stack) { %>
                                <div class="divider" style="margin: 2rem 0;"></div>
                                <div style="background: #f5f5f5; padding: 1rem; border-radius: 4px;">
                                    <pre style="margin: 0; white-space: pre-wrap;"><%= error.stack %></pre>
                                </div>
                            <% } %>
                            <div class="center-align" style="margin-top: 2rem;">
                                <a href="/" class="btn waves-effect waves-light">
                                    <i class="material-icons left">home</i>
                                    Return Home
                                </a>
                                <button onclick="window.history.back()" class="btn-flat waves-effect">
                                    <i class="material-icons left">arrow_back</i>
                                    Go Back
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <%- include('partials/footer') %>

    <!-- Materialize JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>

</html> 